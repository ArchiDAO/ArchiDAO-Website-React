import React from "react";
import '../styles/HeroSection.css';

const HeroSection = () => {
    return (
        <div className="hero-section-container">Hero Section</div>
    )
}

export default HeroSection;